/*import {
    Outlet, Link, useLoaderData, Form
} from "react-router-dom"
*/
//import functions to get data, create data
// creating an action

/*
const contact = await createData()
return contact
*/
function Root () {

    //const { data } = useLoaderData();
    return(
        <>
        <div>
            <h1> </h1>
        </div>
        </>
    )
}

export default Root